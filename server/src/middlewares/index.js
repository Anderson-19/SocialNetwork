const validateField = require('./validateField');
const validateFile = require('./validateFile');
const validateJWT = require('./validateJWT');

module.exports = { validateField, validateFile, validateJWT }
